# Database Configuration
DB_HOST=dydb2-instance-1.cz8kik28igwg.us-east-1.rds.amazonaws.com
DB_USER=GestionPanificados
DB_PASS=paniFican2la
DB_NAME=Acceso_A_Planta
DB_PORT=3306
DB_TIMEZONE=-03:00

# Server Configuration
PORT=5000
NODE_ENV=development
QR_SECRET=tu_clave_secreta_para_qr_aqui


# Paginación - Cantidad de solicitudes por página en el listado (default: 20)
PAGE_SIZE_SOLICITUDES=20

# Feature Flags (habilitar/deshabilitar funciones)
ENABLE_GOOGLE_LOGIN=true
ENABLE_DOCUMENTS=true
ENABLE_QR=true

# Authentication Configuration (Azure AD)
# Obtenga estos valores del Portal de Azure (App Registrations)
AZURE_AD_CLIENT_ID=your_client_id
AZURE_AD_TENANT_ID=your_tenant_id
AZURE_AD_CLIENT_SECRET=your_client_secret

# Push Notifications (VAPID Keys)
# Generar con: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
